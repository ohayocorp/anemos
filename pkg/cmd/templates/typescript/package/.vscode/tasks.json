{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "anemos build --tsc ${workspaceFolder} ${workspaceFolder}/dist/index.js",
            "windows": {
                "command": "anemos.exe build --tsc ${workspaceFolder} ${workspaceFolder}\\dist\\index.js"
            },
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}